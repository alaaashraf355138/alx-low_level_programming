#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry-level
 *
 * Return: Always 0 (success/correct)
 */

int main(void)
{

	int num;

	for (num = 48; num <= 56; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
