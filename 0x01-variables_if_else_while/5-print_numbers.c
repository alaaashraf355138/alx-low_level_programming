#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry-level
 *
 * Return: Always 0 (success/correct)
 */

int main(void)
{

	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	putchar('\n');
	return (0);
}
